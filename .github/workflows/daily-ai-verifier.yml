name: Daily AI Exam Verifier

on:
  schedule:
    - cron: '0 2 * * *' # 7:30 AM IST daily
  workflow_dispatch: # Manual trigger

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: AI-verify active exams
        uses: actions/github-script@v7
        env:
          API_URL: ${{ secrets.API_URL }}
          BOT_API_KEY: ${{ secrets.BOT_API_KEY }}
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        with:
          script: |
            const apiUrl = process.env.API_URL;
            const botKey = process.env.BOT_API_KEY;
            const geminiKey = process.env.GEMINI_API_KEY;

            if (!apiUrl || !botKey || !geminiKey) {
              core.setFailed('Missing required secrets (API_URL, BOT_API_KEY, GEMINI_API_KEY)');
              return;
            }

            // Fetch active exams
            const fetchRes = await fetch(`${apiUrl}/api/bot/exams?active=true`, {
              headers: { 'x-bot-api-key': botKey }
            });

            if (!fetchRes.ok) {
              core.setFailed(`Failed to fetch exams: ${fetchRes.status}`);
              return;
            }

            const response = await fetchRes.json();
            const exams = response.data || response;

            if (exams.length === 0) {
              core.info('No active exams to verify.');
              return;
            }

            // Pick 3 exams that haven't been verified recently
            const now = new Date();
            const sorted = exams
              .sort((a, b) => {
                const aDate = a.lastVerifiedAt ? new Date(a.lastVerifiedAt) : new Date(0);
                const bDate = b.lastVerifiedAt ? new Date(b.lastVerifiedAt) : new Date(0);
                return aDate - bDate;
              })
              .slice(0, 3);

            core.info(`Verifying ${sorted.length} exams with AI:`);
            for (const e of sorted) {
              core.info(`  - ${e.title} (last verified: ${e.lastVerifiedAt || 'never'})`);
            }

            // Verify each exam with Gemini
            for (const exam of sorted) {
              try {
                const prompt = `You are a government exam data verification assistant. Verify the following Indian government exam and provide ONLY a JSON response.

            Exam: ${exam.title}
            Category: ${exam.category}
            Current Last Date: ${exam.lastDate || 'not set'}
            Current Date Status: ${exam.dateStatus || 'not set'}
            Conducting Body: ${exam.conductingBody || 'not set'}
            Vacancies: ${exam.vacancies || 'not set'}

            Today's date: ${now.toISOString().split('T')[0]}

            Search the web for the latest information about this exam. Check the official website of the conducting body.

            Respond with ONLY this JSON format (no markdown, no explanation):
            {
              "needsUpdate": true/false,
              "updates": {
                "lastDate": "YYYY-MM-DD or null if unknown",
                "dateStatus": "upcoming/ongoing/closed",
                "vacancies": number or null,
                "isActive": true/false
              },
              "confidence": "high/medium/low",
              "reason": "brief explanation of what changed or why no update needed"
            }

            Rules:
            - Only set needsUpdate to true if you are confident the data has changed
            - If the last date has passed, set isActive to false and dateStatus to "closed"
            - Only include fields in updates that actually need changing
            - If unsure, set confidence to "low" and needsUpdate to false`;

                const geminiRes = await fetch(
                  `https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${geminiKey}`,
                  {
                    method: 'POST',
                    headers: { 'Content-Type': 'application/json' },
                    body: JSON.stringify({
                      contents: [{ parts: [{ text: prompt }] }],
                      generationConfig: { temperature: 0.1, maxOutputTokens: 500 }
                    })
                  }
                );

                if (!geminiRes.ok) {
                  core.warning(`Gemini API error for "${exam.title}": ${geminiRes.status}`);
                  continue;
                }

                const geminiData = await geminiRes.json();
                const text = geminiData.candidates?.[0]?.content?.parts?.[0]?.text || '';

                // Extract JSON from response
                const jsonMatch = text.match(/\{[\s\S]*\}/);
                if (!jsonMatch) {
                  core.warning(`No valid JSON in Gemini response for "${exam.title}"`);
                  continue;
                }

                const verification = JSON.parse(jsonMatch[0]);
                core.info(`AI result for "${exam.title}": needsUpdate=${verification.needsUpdate}, confidence=${verification.confidence}, reason=${verification.reason}`);

                if (!verification.needsUpdate || verification.confidence === 'low') {
                  // Still mark as verified even if no changes needed
                  await fetch(`${apiUrl}/api/bot/update`, {
                    method: 'POST',
                    headers: {
                      'Content-Type': 'application/json',
                      'x-bot-api-key': botKey
                    },
                    body: JSON.stringify({
                      title: exam.title,
                      category: exam.category,
                      updates: {},
                      source: 'gemini-ai-verifier'
                    })
                  });
                  core.info(`  Marked as verified (no changes needed)`);
                  continue;
                }

                // Apply updates
                const updates = verification.updates || {};
                const updateRes = await fetch(`${apiUrl}/api/bot/update`, {
                  method: 'POST',
                  headers: {
                    'Content-Type': 'application/json',
                    'x-bot-api-key': botKey
                  },
                  body: JSON.stringify({
                    title: exam.title,
                    category: exam.category,
                    updates,
                    source: 'gemini-ai-verifier'
                  })
                });

                if (updateRes.ok) {
                  const result = await updateRes.json();
                  core.info(`  Updated: ${JSON.stringify(result.data?.fieldsUpdated || [])}`);
                } else {
                  core.warning(`  Update failed: ${updateRes.status}`);
                }

                // Rate limit: wait 2 seconds between Gemini calls
                await new Promise(r => setTimeout(r, 2000));

              } catch (err) {
                core.warning(`Error verifying "${exam.title}": ${err.message}`);
              }
            }

            core.info('AI verification complete.');
