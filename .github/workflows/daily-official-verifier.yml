name: Daily Official-Source Verifier

# Verifies a batch of exams each day against their OWN official websites using
# AI. Critical fields (dates, status, vacancies, fee) are queued to the admin
# review queue instead of being auto-applied; safe text fields update directly.
# Runs in batches (oldest-verified first) so all exams cycle over several days.

on:
  schedule:
    # Three passes at 07:15, 07:45 and 08:15 UTC, all just after 07:00 UTC —
    # midnight Pacific, when the Gemini free-tier daily quota resets.
    #
    # The hour matters: running at 22:30 produced 10 x gemini_rate_limit on 12
    # exams two days running, because the other verifier's passes had already
    # drawn the quota down by then. Moving to 07:15 cut that to 3 on 2026-08-19.
    # This is the higher-fidelity check — it reads the exam's real government
    # page rather than asking the model what it recalls — so it takes fresh
    # quota first and the other verifier, which starts at 09:00, gets the rest.
    #
    # Three passes rather than one because the binding constraint is now the
    # 3.5-minute batch deadline, not quota: a run reaches about six exams, and
    # one pass a day against a ~413-exam catalogue would take months to cycle.
    # Exams deferred at the deadline keep their older lastVerifyAttemptAt and
    # sort to the front of the next run, so consecutive passes resume where the
    # last stopped instead of re-checking the same six.
    - cron: '15,45 7 * * *'
    - cron: '15 8 * * *'
  workflow_dispatch:
    inputs:
      limit:
        description: 'How many exams to verify this run'
        required: false
        default: '15'
        type: string

permissions:
  contents: read

jobs:
  verify-official:
    runs-on: ubuntu-latest

    steps:
      - name: Verify exams against official websites
        id: verify
        uses: actions/github-script@v7
        env:
          API_URL: ${{ secrets.API_URL }}
          BOT_API_KEY: ${{ secrets.BOT_API_KEY }}
          LIMIT: ${{ github.event.inputs.limit || '15' }}
        with:
          script: |
            const apiUrl = process.env.API_URL;
            const botKey = process.env.BOT_API_KEY;
            const limit = parseInt(process.env.LIMIT, 10) || 15;

            if (!apiUrl || !botKey) {
              core.warning('API_URL or BOT_API_KEY not configured — skipping.');
              return;
            }

            core.info(`Verifying ${limit} exams against their official websites...`);
            try {
              const res = await fetch(`${apiUrl}/api/bot/verify-official-sources`, {
                method: 'POST',
                headers: { 'x-bot-api-key': botKey, 'Content-Type': 'application/json' },
                body: JSON.stringify({ limit }),
              });
              if (!res.ok) {
                core.warning(`Official-verify API returned ${res.status}`);
                return;
              }
              const data = await res.json();
              const stats = data.data || {};

              core.info(`\n${'═'.repeat(50)}`);
              core.info('  Official-Source Verification Report');
              core.info(`${'═'.repeat(50)}`);
              core.info(`  Checked            : ${stats.checked ?? '—'}`);
              core.info(`  Applied (safe)     : ${stats.applied ?? '—'}`);
              core.info(`  Queued for review  : ${stats.queued ?? '—'}`);
              core.info(`  Skipped (no page)  : ${stats.skipped ?? '—'}`);
              core.info(`  Errors             : ${stats.errors ?? '—'}`);
              // Deep-linking replaces the conducting body's shared homepage with
              // the exam's own notification page where one can be identified
              // confidently. Reported because "how often did that work" is the
              // number that says whether verification is actually looking at
              // exam-specific content or still reading a portal landing page.
              core.info(`  Used exam's own page: ${stats.deepLinked ?? '—'} (fell back to homepage: ${stats.deepLinkFailed ?? '—'})`);
              if (stats.stoppedEarly) {
                core.info(`  NOTE: stopped at the batch deadline; ${stats.remaining} exam(s) deferred to the next run.`);
              }
              core.info(`${'═'.repeat(50)}`);

              // A bare "skipped: 7" isn't actionable — each cause needs a
              // different fix (bot-blocking vs dead URL vs PDF-only page vs a
              // JS-rendered portal that loads but yields no text). Print the
              // breakdown and name the offending sites so the pattern is
              // visible here rather than only in the Render logs.
              if (stats.skipReasons && Object.keys(stats.skipReasons).length) {
                core.info('  Why exams were skipped:');
                for (const [reason, count] of Object.entries(stats.skipReasons)) {
                  core.info(`    ${count} x ${reason}`);
                }
              }
              if (Array.isArray(stats.skippedDetail) && stats.skippedDetail.length) {
                core.info('  Skipped sites:');
                for (const d of stats.skippedDetail) {
                  core.info(`    - ${d.title} — ${d.url} — ${d.reason}`);
                }
              }
              if (stats.errorReasons && Object.keys(stats.errorReasons).length) {
                core.info('  Error causes:');
                for (const [reason, count] of Object.entries(stats.errorReasons)) {
                  core.info(`    ${count} x ${reason}`);
                }
              }
              core.info(`${'═'.repeat(50)}\n`);

              core.setOutput('applied', String(stats.applied || 0));
              core.setOutput('queued', String(stats.queued || 0));
            } catch (err) {
              core.warning(`Official verification failed: ${err.message}`);
            }

      - name: Summary
        run: |
          echo "### Official-Source Verification Complete" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "| Metric | Value |" >> $GITHUB_STEP_SUMMARY
          echo "|--------|-------|" >> $GITHUB_STEP_SUMMARY
          echo "| Fields applied (safe) | ${{ steps.verify.outputs.applied || '0' }} |" >> $GITHUB_STEP_SUMMARY
          echo "| Fields queued for review | ${{ steps.verify.outputs.queued || '0' }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "Review queued items in the admin [Verification Dashboard](/admin/verification)." >> $GITHUB_STEP_SUMMARY
